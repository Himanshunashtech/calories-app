// This page is intentionally left blank or returns null
// to avoid conflicts with the main root page (src/app/page.tsx).
// If you encounter a "parallel routes" error related to src/app/(app),
// please try deleting this file src/app/(app)/page.tsx entirely.

export default function AppRootPage() {
  return null;
}
